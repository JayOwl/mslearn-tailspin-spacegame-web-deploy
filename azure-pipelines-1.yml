# ASP.NET Core
# Build and test ASP.NET Core projects targeting .NET Core.
# Add steps that run tests, create a NuGet package, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- master

pool:
  name: MVCLocalPool
  demands:
  - agent.name -equals MvsAgent01


stages:
- stage: A
  displayName: 'SafetyLine1'
  variables:
  - name: ApplicationInsightsInstrumentationKey     
    value: '%ApplicationInsightsInstrumentationKey%'
  jobs:
  - job: A 
    steps:
    - script: 'echo Write your commands here Job A   %ApplicationInsightsInstrumentationKey%'

- stage: B
  displayName: 'SafetyLine2'
  dependsOn:
  - A
  jobs:
  - job: B 
    steps:
    - script: 'echo Write your commands here Job B   %ApplicationInsightsInstrumentationKey%, $(ApplicationInsightsInstrumentationKey)'

# stage C runs if B succeeds
- stage: C
  dependsOn:
  - B
  condition: succeeded('B')
  variables:
    - name: ApplicationInsightsInstrumentationKey     
      value: '%ApplicationInsightsInstrumentationKey2%'
  jobs:
  - job: C 
    steps:
    - script: 'echo Write your commands here Job C   %ApplicationInsightsInstrumentationKey2%'